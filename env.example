# Copy this file to .env and fill in your values.
# Never commit .env to git.

# Database (use MongoDB Atlas connection string in production)
MONGO_URI=mongodb://127.0.0.1:27017/shopsphere

# JWT (use a long random string in production, e.g. 64+ chars)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d

# Server
PORT=5000
NODE_ENV=development

# Frontend URL for CORS (production: https://yourdomain.com or comma-separated for multiple)
FRONTEND_URL=http://localhost:3000

# Email (for order confirmation etc.) - required in production for emails to work
# Gmail: use App Password (not regular password), see PRODUCTION_EMAIL_SETUP.md
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
# Optional: custom "from" address (defaults to EMAIL_USER)
# EMAIL_FROM=noreply@yourdomain.com

# Optional: client/business owner gets a BCC copy of every order (e.g. your Gmail)
# CLIENT_EMAIL=your-business@gmail.com

# Razorpay (for Online Payment) - required in production for payments to work
# Get keys from https://dashboard.razorpay.com/ → API Keys
# RAZORPAY_KEY_ID=rzp_test_xxxx or rzp_live_xxxx
# RAZORPAY_KEY_SECRET=your-key-secret
